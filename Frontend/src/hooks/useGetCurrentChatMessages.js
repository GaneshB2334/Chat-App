import React, { useState } from 'react'

const useGetCurrentChatMessages = () => {
    const [allMsg, setAllMsg] = useState([])
    const [loadingMsg, setLoadingMsg] = useState(false)

    

    return {}
}

export default useGetCurrentChatMessages
